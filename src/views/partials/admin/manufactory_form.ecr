<form action="<%= form_action %>" accept-charset="utf8" method="post">
  <div class="field">
    <label for="manufactory_name">名称</label>
    <input id="manufactory_name" type="text" name="manufactory[name]", value="<%= operation.name.value %>" />
  </div>

  <div style="color: red">
    <% errors.each do |name, errors| %>
      <%= name %> <%= errors.join(", ") %><br />
    <% end %>
  </div>

  <div class="actions">
    <input type="submit" value="提交" />
  </div>
</form>
