<form action="<%= form_action %>" accept-charset="utf8" method="post">
  <div class="field">
    <label for="process_line_name">名称</label>
    <input id="process_line_name" type="text" name="process_line[name]", value="<%= operation.name.value %>" />
  </div>

  <div style="color: red">
    <% errors.each do |name, errors| %>
      <%= name %> <%= errors.join(", ") %><br />
    <% end %>
  </div>

  <div class="actions">
    <input type="submit" value="提交" />
  </div>
</form>
